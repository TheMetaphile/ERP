name: untitled
description: "A unified Flutter project for student and teacher modules."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  auto_size_text: ^3.0.0
  fl_chart: ^0.69.2
  permission_handler: ^11.3.1
  http: ^1.2.2
  shared_preferences: ^2.3.3
  loading_animation_widget: ^1.3.0
  workmanager: ^0.5.2
  google_nav_bar: ^5.0.7
  flutter_sound: ^9.17.8
  audio_session: ^0.1.21
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0
  porcupine_flutter: ^3.0.3
  highlight_text: ^1.8.0
  flutter_otp_text_field: ^1.2.0
  flutter_svg: ^2.0.16
  syncfusion_flutter_calendar: ^27.2.5
  page_transition: ^2.1.0
  flutter_staggered_grid_view: ^0.7.0
  shimmer: ^3.0.0
  flutter_animate: ^4.5.2
  sn_progress_dialog: ^1.1.4
  intl: ^0.18.0
  flutter_staggered_animations: ^1.1.1
  animated_text_kit: ^4.2.2
  confetti: ^0.8.0
  firebase_core: ^3.8.1
  firebase_messaging: ^15.1.6
  flutter_local_notifications: ^18.0.1
  razorpay_flutter: ^1.4.0
  # Uncomment if needed
  # audioplayers: ^6.1.0
  # alphabet_slider: ^1.0.4
  # image_picker: ^0.8.4+4
  # fluttertoast: ^8.2.4
  # provider: ^6.1.2
  # cached_network_image: ^3.4.1
  # flutter_typeahead: ^5.2.0
  # usage_stats: ^1.2.0
  # animated_background: ^2.0.0
  # lottie: ^1.4.3
  # molten_navigationbar_flutter: ^1.0.0
  # chatview: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_snowboy:
    git:
      url: "https://github.com/mideind/FlutterSnowboy.git"

dependency_overrides:
  win32: ^5.9.0

flutter:
  uses-material-design: true

  assets:
    - assets/Images/
    - assets/onBoarding/
    - assets/onBoarding/Forget/
    - assets/onBoarding/Intro/
    - assets/onBoarding/Login/
    - assets/onBoarding/NewPassword/
    - assets/onBoarding/VerifyEmail/
    - assets/Images/Admin Drawer/
    - assets/Images/Admin Home/
    - assets/Images/Teacher Navigation/
    - assets/Images/TeacherDashboard/
    - assets/Images/Class Activity/
    - assets/DashboardImages/
    - assets/

  # fonts: # Uncomment and add fonts if needed
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
